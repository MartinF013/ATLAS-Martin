import {
  l
} from "./chunk-T7BEWVV3.js";

// node_modules/@arcgis/core/views/3d/interactive/visualElements/VisualElement.js
var t = class {
  constructor(t2) {
    this._attached = false, this._resourcesCreated = false, this._visible = true, this.view = t2, this._handle = l(() => this.view.ready, (e) => {
      this._resourcesCreated && (e ? this._createResources() : this._destroyResources());
    });
  }
  applyProps(e) {
    let t2 = false;
    for (const s in e)
      s in this ? "attached" === s ? t2 = e[s] : this[s] = e[s] : console.error("Cannot set unknown property", s);
    this.attached = t2;
  }
  destroy() {
    this.attached = false, this._handle.remove();
  }
  get attached() {
    return this._attached;
  }
  set attached(e) {
    e !== this._attached && this.view._stage && (this._attached = e, this._attached && !this._resourcesCreated ? this._createResources() : !this._attached && this._resourcesCreated && this._destroyResources());
  }
  get visible() {
    return this._visible;
  }
  set visible(e) {
    e !== this._visible && (this._visible = e, this.attached && this.updateVisibility(e));
  }
  _createResources() {
    this.createResources(), this._resourcesCreated = true, this.visible || this.updateVisibility(false);
  }
  _destroyResources() {
    this.destroyResources(), this._resourcesCreated = false;
  }
};

export {
  t
};
//# sourceMappingURL=chunk-JYWDI7GA.js.map
