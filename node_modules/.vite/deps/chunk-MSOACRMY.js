import {
  o as o2
} from "./chunk-UV5UBITC.js";
import {
  r
} from "./chunk-HRAEK6P4.js";
import {
  e as e3,
  t
} from "./chunk-SV4VP57N.js";
import {
  o
} from "./chunk-XEIRRA3E.js";
import {
  e as e2
} from "./chunk-46N7XS5M.js";
import {
  i2 as i
} from "./chunk-I5UNY2WQ.js";
import {
  n
} from "./chunk-NAB3NF54.js";
import {
  O
} from "./chunk-VMF4NMEB.js";
import {
  e
} from "./chunk-IVBXK6OP.js";

// node_modules/@arcgis/core/views/3d/environment/PrecipitationTechniqueConfiguration.js
var t2;
!function(e4) {
  e4[e4.Rain = 0] = "Rain", e4[e4.Snow = 1] = "Snow", e4[e4.COUNT = 2] = "COUNT";
}(t2 || (t2 = {}));
var i2 = class extends t {
  constructor() {
    super(...arguments), this.type = t2.Rain;
  }
};
e([e3({ count: t2.COUNT })], i2.prototype, "type", void 0);

// node_modules/@arcgis/core/chunks/Precipitation.glsl.js
function d(d2) {
  const c2 = new i();
  return c2.attributes.add(O.POSITION, "vec3"), c2.attributes.add(O.INSTANCEFEATUREATTRIBUTE, "float"), c2.vertex.uniforms.add(new o2("cameraPosition")), c2.vertex.uniforms.add(new o2("offset")), c2.vertex.uniforms.add(new r("width")), c2.vertex.uniforms.add(new e2("proj", (e4, t3) => t3.camera.projectionMatrix)), c2.vertex.uniforms.add(new o("view")), c2.vertex.uniforms.add(new r("time")), c2.varyings.add("vUv", "vec2"), c2.vertex.code.add(n`
    vec3 hash31(float p){
      vec3 p3 = fract(vec3(p) * vec3(0.1031, 0.1030, 0.0973));
      p3 += dot(p3, p3.yzx + 33.33);
      return fract((p3.xxy + p3.yzz) * p3.zyx);
    }

    float hash11(float p){
      p = fract(p * 0.1031);
      p *= p + 33.33;
      p *= p + p;
      return fract(p);
    }

    //https://www.geeks3d.com/20141201/how-to-rotate-a-vertex-by-a-quaternion-in-glsl/
    vec3 rotateVectorByQuaternion(vec3 v, vec4 q){
      return 2.0 * cross(q.xyz, v * q.w + cross(q.xyz, v)) + v;
    }

    void main(void) {

      vUv = position.xz;

      vec3 rand = hash31(instanceFeatureAttribute);

      // Set random position for all particles
      // The hash function space is not high resolution so offset particles by an additional random value
      // This creates grids of 1000 particles which are shifted by random hundreths of the tile width
      // overlaying multiple identical but offset grids
      vec3 randomPosition = 2.0 * (rand + (0.01 + 0.01 * rand) * floor(0.001 * instanceFeatureAttribute)) - 1.0;

      // Random orientation of rain drops
      float angle = 3.1415 * hash11(instanceFeatureAttribute);

      vec3 up = vec3(0, 0, 1);

      // Gravity and wind direction
      vec3 direction = normalize(cameraPosition);

      vec3 tangent = normalize(cross(direction, up));

      // Gravity
      vec3 animatedPos = randomPosition + direction * -time;

      // Rain particles fall straight down and are randomly oriented
      // Snow particles have random sinusoid trajectories and are rotated to face the camera
      ${d2.type === t2.Rain ? n`
            // Random rotation for particle
            vec3 rotationAxis = up;
            vec4 quat = vec4(rotationAxis * sin(angle), cos(angle));
            vec3 transformedPos = rotateVectorByQuaternion(vec3(0.2, 0.2, 4.0) * (position - vec3(0.5, 0.0, 0.5)), quat);

            // Rotate particle to planetary position
            rotationAxis = tangent;
            angle = 0.5 * -acos(dot(direction, up));
            quat = vec4(rotationAxis * sin(angle), cos(angle));
            transformedPos = rotateVectorByQuaternion(transformedPos, quat);

            vec4 pos = mat4(mat3(view)) * vec4(transformedPos + (mod(width * animatedPos - offset, width) - 0.5 * width), 1.0);
            gl_Position = proj * pos;
      ` : n`
            vec3 rotationAxis = direction;
            vec4 quat = vec4(rotationAxis * sin(angle), cos(angle));

            tangent = rotateVectorByQuaternion(tangent, quat);
            // Random sinusoid from friction
            animatedPos += tangent * 0.25 * sin(dot(animatedPos, direction));
            vec4 pos = mat4(mat3(view)) * vec4((mod(width * animatedPos - offset, width) - 0.5 * width), 1.0);
            gl_Position = proj * (0.5 * vec4(position.xzy, 0.0) + pos);
      `}
    }
  `), c2.fragment.uniforms.add(new r("opacity")), c2.fragment.uniforms.add(new o2("particleColor")), c2.fragment.code.add(n`
    void main() {

      // Cut off corners of the triangle
      if(vUv.x < 0.0 || vUv.y < 0.0){
        discard;
      }

      float d = length(vUv - vec2(0.5));

      ${d2.type === t2.Rain ? n`d = 0.35 * smoothstep(0.5, 0.0, d);` : n`d = smoothstep(0.5, 0.1, d);`}
      gl_FragColor = opacity * vec4(particleColor * d, d);
    }
  `), c2;
}
var c = Object.freeze(Object.defineProperty({ __proto__: null, build: d }, Symbol.toStringTag, { value: "Module" }));

export {
  t2 as t,
  i2 as i,
  d,
  c
};
//# sourceMappingURL=chunk-MSOACRMY.js.map
